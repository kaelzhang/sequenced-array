{
  "name": "sequenced-array",
  "version": "2.0.0",
  "description": "The sequenced array class which maintains sorted order with time complexity O(logN)",
  "main": "src/index.js",
  "scripts": {
    "test": "nyc ava --timeout=10s",
    "test:dev": "NODE_DEBUG=sequenced-array nyc ava --timeout=10s --verbose",
    "lint": "eslint .",
    "posttest": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "files": [
    "src/"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/kaelzhang/sequenced-array.git"
  },
  "keywords": [
    "sequenced-array",
    "array",
    "order",
    "sorted order",
    "desc",
    "asc"
  ],
  "engines": {
    "node": ">= 6"
  },
  "author": "kaelzhang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kaelzhang/sequenced-array/issues"
  },
  "devDependencies": {
    "ava": "^3.8.2",
    "codecov": "^3.7.0",
    "nyc": "^15.0.1",
    "eslint-config-ostai": "^3.0.0",
    "eslint-plugin-import": "^2.20.2",
    "eslint": "^7.0.0"
  }
}
